using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Conditionals
{
    public class IfThenElseIfElse : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
            //if (true)
            //{

            //}
            //else if (false)
            //{

            //}
            //else
            //{

            //}
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
